let
	type string_arr = array of string
	var a : string_arr := new string_arr [10] of ""
in
  for i := 0 to 9 do
    let
      var c := chr(97 + i)
    in 
     a[i] := c
    end;

  for i := 0 to 9 do
    let
      var c := chr(97 + i)
    in 
     print(a[i])
    end
end

